<script lang="ts">
  // v1 skeleton: this shell just renders a banner and loads in a browser.
  // The Connect button, CO₂-hero (Live reading), and timeseries chart arrive
  // in later tickets (09 transport, 10 UI). Correctness-critical logic — the
  // slot-keyed Merge (ticket 08) — lives in pure `src/lib` modules under test.
</script>

<main>
  <h1>kuuki-pod</h1>
  <p>Air-quality Pod dashboard — skeleton. UI arrives in later tickets.</p>
</main>

<style>
  main {
    text-align: center;
    padding: 2rem;
  }

  h1 {
    margin: 0 0 0.25rem;
    font-size: 2.5rem;
    letter-spacing: -0.02em;
  }

  p {
    margin: 0;
    opacity: 0.7;
  }
</style>
