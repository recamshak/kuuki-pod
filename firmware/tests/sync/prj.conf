# Host-run ztest configuration for the Sync seam. No hardware, no BLE —
# sync_iter, sync_encode_record() and sync_records_per_notification() are pure
# functions over a Buffer and a plain struct sync_record.
CONFIG_ZTEST=y
