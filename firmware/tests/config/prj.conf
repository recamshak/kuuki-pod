# Host-run ztest configuration for the project parameters. No hardware, no
# drivers: the suite only reads CONFIG_KUUKI_* and the capacity derived from it,
# at the defaults the application ships with.
CONFIG_ZTEST=y
